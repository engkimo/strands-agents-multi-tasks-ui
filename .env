# Backend
DB_PATH=var/data/app.db
DATA_DIR=var/data
DEMO_MODE=1

# Tool commands (leave empty to use fallback echo)
# Each *_USE_STDIN=1 means send prompt via STDIN. Set 0 if the CLI takes prompt as args.
TOOL_CLAUDE_CODE_CMD=
TOOL_CLAUDE_CODE_ARGS=
TOOL_CLAUDE_CODE_USE_STDIN=1

TOOL_CODEX_CLI_CMD=
TOOL_CODEX_CLI_ARGS=
TOOL_CODEX_CLI_USE_STDIN=1

TOOL_GEMINI_CLI_CMD=
TOOL_GEMINI_CLI_ARGS=
TOOL_GEMINI_CLI_USE_STDIN=1

## Spec Kit は今回のワークフローから除外（未使用）
#TOOL_SPEC_KIT_CMD=
#TOOL_SPEC_KIT_ARGS=
#TOOL_SPEC_KIT_USE_STDIN=1

# UI
# Use Vite dev server proxy to avoid host port issues
VITE_API_BASE=/api

# Common API Keys (forwarded to tool subprocesses)
# 実CLIを使う場合にのみ設定（DEMO_MODE=0）。ここには値を直接書かず、
# 手元の環境でエクスポートしてから `docker compose` を実行することを推奨します。
# 例: export ANTHROPIC_API_KEY=...; export OPENAI_API_KEY=...
# 以下は空のままにしました。
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GOOGLE_API_KEY=
GEMINI_API_KEY=
